language: ruby
before_install:
  - rvm install 2.3.1
  - gem update --system
  - gem --version
rvm:
  - 2.3
sudo: false
cache: bundler
before_script:
  - sh -e /etc/init.d/xvfb start
  - export DISPLAY=:99.0
  - bundle exec rake test_app
script:
  - bundle exec rspec spec
